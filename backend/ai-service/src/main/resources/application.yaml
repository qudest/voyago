spring:
  application:
    name: ai-service

  cloud:
    openfeign:
      client:
        config:
          account-service.url: ${ACCOUNT_SERVICE}
google:
  maps:
    api-key: ${GOOGLE_API-KEY}

server:
  port: ${APP_PORT:8090}
